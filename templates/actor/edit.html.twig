{% extends 'base.html.twig' %}

{% block title %}{{ 'title_edit_actor'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'edit.actor'|trans }}</h1>
        {{ include('actor/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        <div class="mt-3">{{ include('actor/_delete_form.html.twig') }}</div>
    <div class="d-flex justify-content-center mt-5">
        <button class="text-dark btn btn-warning">
            <a class="text-dark text-decoration-none" href="{{ path('actor_index') }}">{{ 'back'|trans }}</a>
        </button>
    </div>
{% endblock %}
