{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <h1>Éditer un Episode</h1>
    {{ include('episode/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <div class="mt-3">{{ include('episode/_delete_form.html.twig') }}</div>
    <div class="d-flex justify-content-center mt-5">
        <button class="btn btn-warning">
            <a class="text-dark text-decoration-none" href="{{ path('episode_index') }}">Retour</a>
        </button>
    </div>
{% endblock %}
