
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="{{ path('app_index') }}">Wild Séries</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('wild_index') }}">Séries<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('category_index') }}">Catégories<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('actor_index') }}">Acteurs<span class="sr-only">(current)</span></a>
                </li>
            </ul>
           <div class="text-white mr-2">
            {% if app.user %}
                Hello {{ app.user.email }}
                <a class="text-danger text-decoration-none ml-4" href="{{ path('app_logout') }}">Se déconnecter</a>
            {% else %}
                <a class="text-success text-decoration-none" href="{{ path('app_login') }}">Se connecter</a>
            {% endif %}
            </div>
        </div>
    </nav>
