{% extends 'base.html.twig' %}

{% block title %}{{ 'title_edit_program'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'edit.program'|trans }}</h1>
    {{ include('program/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <div class="mt-3">{{ include('program/_delete_form.html.twig') }}</div>
    <div class="d-flex justify-content-center mt-5">
        <button class="btn btn-warning">
            <a class="text-dark text-decoration-none" href="{{ path('program_index') }}">{{ 'back'|trans }}Retour</a>
        </button>
    </div>
{% endblock %}
