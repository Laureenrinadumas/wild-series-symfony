{% extends 'base.html.twig' %}

{% block title %}Edit Program{% endblock %}

{% block body %}
    <h1>Éditer un programme</h1>
    {{ include('program/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <div class="mt-3">{{ include('program/_delete_form.html.twig') }}</div>
    <div class="d-flex justify-content-center mt-5">
        <button class="btn btn-warning">
            <a class="text-dark text-decoration-none" href="{{ path('program_index') }}">Retour</a>
        </button>
    </div>
{% endblock %}
