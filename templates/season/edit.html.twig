{% extends 'base.html.twig' %}

{% block title %}{{ 'edit_seasons'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'edit.season'|trans }}</h1>
    {{ include('season/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <div class="mt-3">{{ include('season/_delete_form.html.twig') }}</div>
    <div class="d-flex justify-content-center">
        <button class="btn btn-warning">
            <a class="text-dark text-decoration-none" href="{{ path('season_index') }}">{{ 'back'|trans }}Retour</a>
        </button>
    </div>
{% endblock %}
