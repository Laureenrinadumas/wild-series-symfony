{% extends 'base.html.twig' %}

{% block title %}Edit Season{% endblock %}

{% block body %}
    <h1>Éditer une saison</h1>
    {{ include('season/_form.html.twig', {'button_label': 'Enregistrer'}) }}
    <div class="mt-3">{{ include('season/_delete_form.html.twig') }}</div>
    <div class="d-flex justify-content-center">
        <button class="btn btn-warning">
            <a class="text-dark text-decoration-none" href="{{ path('season_index') }}">Retour</a>
        </button>
    </div>
{% endblock %}
